#Mode ClusterIP
apiVersion: v1
kind: Service
metadata:
  name: -
spec:
  selector:
    app: -
  type: ClusterIP
  ports:
  - port: -
    targetPort: -

---

#Mode NodePort
apiVersion: v1
kind: Service
metadata:
  name: -
spec:
  selector:
    app: -
  type: NodePort
  ports:
  - port: -
    targetPort: -
    nodePort: -